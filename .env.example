# Copy to .env and fill in only the values you need, then set USE_LIVE_APIS = True
# in the script(s) you're running. All optional in mock mode (the default).

COHERE_API_KEY=
PINECONE_API_KEY=
PINECONE_INDEX_NAME=claims-fraud-cases
ZAPIER_WEBHOOK_URL=
ANTHROPIC_API_KEY=
